# Report

The final submission should be a PDF written in PhysRev style. `main.tex` is a RevTeX starter file
with the expected sections already in place.

Before final submission:

- Add the GitHub repository link.
- Insert final figures from `figures/`.
- Replace notes with final prose.
- Compile to PDF.
